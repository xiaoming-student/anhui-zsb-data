name: P0 Batch 01 Superseded

on:
  push:
    branches:
      - agent/data-p0-batch-01
    paths:
      - .github/workflows/p0-batch-01-acquire.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: p0-batch-01-${{ github.ref }}
  cancel-in-progress: true

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by agent/data-p0-batch-01-targeted"
